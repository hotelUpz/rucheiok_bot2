## TECH_DEBT.md

1. Нужно перестроить бота под новую конфигурацию и новый порядок аргумента сигналов. Теперь главным сигналом будет сигнал main_spread_pattern. Условие для лонга (бинансПрайс - пхемексПрайс) / пхемексПрайс >= spread_to_entry_pct, для шорта (бинансПрайс - пхемексПрайс) / пхемексПрайс <= -spread_to_entry_pct
Вспомагательные сигналы -- фильтр стакана, фильтры фандинга.

Источник цен для main_spread_pattern -- рест данные с пхемекс и бинанса горячих цен. Класс -- PriceCacheManager, лупа становится богословским ядром а не придатком в утилите. Возможно ее есть смысл вынести в другое место кода. И она становится высокочастотной (частично по этой причине перешли на curl_cffi), нет, на вебсокет переходить не будем ибо стрим у пхемекс гнилой. Использовать стакан для этой цели в дальнейшем -- возможно, но сейчас не будем, максимум упростим модель получения цен.

2. В base scenario будем прокидывать процентный спред (тот самый который участвовал в принятии решения о входе -- main_spread_pattern), считаем текущий и проверяем условие 
        # 1. Base checking:
        if pos.side == "LONG":
            if current_price_spread >= self.spread_to_exit_pct:
                return None
        else:
            if current_price_spread <= self.spread_to_exit_pct:
                return None

                -- я все частично настроил. Надо согласовать сигнатуры вызова scen_base_analyze. И scen_base_analyze, так и быть, будет возвращать цену стакана куда можно кинуть закрывающую лимитку.

3. Все остальное осталось из старой архитектуры ручейка бота, -- вроде по уму и менять не надо. Проверь чтобы после переписа все архитектура была согласована. Еще. В старом коде не работала интерференция то етсь скупка помех. Это не кракраеугольный камень нашей новой системы, однако надлежит исправить если есть проблемы (ну без передела мира).

4. В Тг я добавил несколько новых кнопок. Проверишь чтобы согласовалось с текущим состоянием проекта.

5. Еще добавь отправку отчетов, как в другом боте, смотри по ссылке https://raw.githubusercontent.com/hotelUpz/uranus_bot/refs/heads/master/CORE/orchestrator.py